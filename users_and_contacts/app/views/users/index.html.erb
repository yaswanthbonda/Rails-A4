<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.notes %></td>
        <td><%= link_to 'Show my contacts list', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<h3>
If you are a new user, you can start a new contacts page of yours by clicking this below link </h3>
<%= link_to 'New User', new_user_path %>
<br>

<br> <br>
<h3>Go to this link below to add a new contact into your contacts list, so please use your id in the first entry of the list. </h3>

<%= link_to 'link', contacts_path %>